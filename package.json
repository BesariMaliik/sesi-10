{
  "name": "webtests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "cobatest": "mocha cobatest --recursive --timeout 60000",
    "test": "mocha test --recursive --reporter mochawesome --timeout 60000",
    "jalanin": "mocha jalanin --recursive --reporter mocha-simple-html-reporter --reporter-options output=report.html --timeout 60000"
  },
   "dependencies": {
        "selenium-webdriver": "^4.17.0",
        "node-fetch": "^3.3.2",
        "ajv": "^8.12.0"
    },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.16.0",
    "mochawesome": "^7.1.3",
    "mocha-simple-html-reporter": "^2.0.0"
  }
}
